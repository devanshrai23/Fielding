# Configuration file of FAR training experiment

# ========== Cluster configuration ========== 
# ip address of the parameter server (need 1 GPU process)
ps_ip: localhost

# ip address of each worker:# of available gpus process on each gpu in this node
# Note that if we collocate ps and worker on same GPU, then we need to decrease this number of available processes on that GPU by 1
# E.g., master node has 4 available processes, then 1 for the ps, and worker should be set to: worker:3
worker_ips:
    - localhost:[4,0]

exp_path: $FEDSCALE_HOME/fedscale/cloud

# Entry function of executor and aggregator under $exp_path
executor_entry: execution/executor.py

aggregator_entry: aggregation/aggregator.py

auth:
    ssh_user: ""
    ssh_private_key: ~/.ssh/id_ed25519

setup_commands:
    - source $HOME/miniconda3/bin/activate fedscale

# ========== Additional job configuration ========== 
# Default parameters are specified in config_parser.py, wherein more description of the parameter can be found

job_conf: 
    - job_name: waymo_classified                   # Generate logs under this folder: log_path/job_name/time_stamp
    - log_path: $FEDSCALE_HOME/benchmark/logs/waymo_classified
    - num_participants: 20
    - data_set: waymo_classified                     # Dataset: e.g., openImg, google_speech, stackoverflow
    - data_dir: $FEDSCALE_HOME/benchmark/dataset/data/waymo_classified    # Path of the dataset
    - data_map_file: $FEDSCALE_HOME/benchmark/dataset/data/waymo_classified/client_data_mapping/train.csv              # Allocation of data to each client, turn to iid setting if not provided
    - test_data_map_file: $FEDSCALE_HOME/benchmark/dataset/data/waymo_classified/client_data_mapping/test.csv
    - data_frame_file: $FEDSCALE_HOME/benchmark/dataset/data/waymo_classified/client_data_mapping/train_data_frame.pkl
    - test_data_frame_file: $FEDSCALE_HOME/benchmark/dataset/data/waymo_classified/client_data_mapping/test_data_frame.pkl
    - client_start_end_rounds_file: $FEDSCALE_HOME/workspace/records/waymo_classified/client_rank_2_start_end_round.pkl
    - device_conf_file: $FEDSCALE_HOME/benchmark/dataset/data/device_info/client_device_capacity
    - model: vit_b16
    - model_zoo: fedscale-torch-zoo
    - eval_interval: 1                     # How many rounds to run a testing on the testing set
    - gradient_policy: fed-prox                 # {"fed-yogi", "fed-prox", "fed-avg"}, "fed-avg" by default
    - rounds: 101
    - filter_less: 11                       # Remove clients w/ less than 21 samples
    - num_loaders: 2
    - yogi_eta: 3e-3 
    - yogi_tau: 1e-8
    - local_steps: 20
    - learning_rate: 0.001
    - min_learning_rate: 0.000001
    - loss_decay: 0.05
    - batch_size: 20
    - test_bsz: 20
    - use_cuda: True
    - cuda_device: cuda:0
    - save_checkpoint: True
    - sample_mode: random
    - data_mode: sliding_window_cumulate
    - wandb_token: [Your_Wandb_Token]
    - split_round: 1
    - adjust_cluster_selected_num: True
    - max_cluster_size_ratio: 0.95
    - recluster_at_drift: True
    - proxy_mu: 0.1
    - dump_per_client_accuracy: True
    - ps_port: 5100
    - use_l1_distance: True # use l1 distance to calculate the distance between two distribution vectors (disable this if you want to use Jensen–Shannon distance)
    # Enable the following two lines to trigger global reclustering based on pairwise distance
    # - use_pairwise_delta_threshold: True
    # - base_delta: 0.1
    # Enable the following two lines to use gradient based clustering with shared model
    # - use_gradient_cluster: True
    # - embedding_model: resnet18
    # Enable the following two lines together with the two lines above to use embedding based clustering with pre-trained model
    # - representation_model: $FEDSCALE_HOME/workspace/checkpoint/waymo_classified/model_resnet18_cluster0.pth 
    # - get_projection: True
